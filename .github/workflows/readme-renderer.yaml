on:
  push:
    branches: master

name: render readme

permissions:
  contents: write

jobs:
  render:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - uses: r-lib/actions/setup-pandoc@v2

    - uses: r-lib/actions/setup-r@v2
      with:
        use-public-rspm: true

    - name: install required packages
      run: Rscript -e 'install.packages(c("rmarkdown"))'

    - name: render README
      run: Rscript -e 'rmarkdown::render("README.Rmd")'

    - name: commit rendered README
      run: |
        git add README.md man/figures/*
        git commit -m "Re-build README.md" || echo "No changes to commit"
        git push origin master || echo "No changes to commit"
# metadata:
# - gpt_version: GPT-5 Codex
# - time: 14:35
# - date: 2026-04-28
# - prompt_used: "Diagnose the failed GitHub README render action and update the workflow to use supported GitHub Actions and R setup steps."
